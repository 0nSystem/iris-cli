//!Here you define the variables required for the request module.

// Defines the variable the nomenclature of the text variable to be translated to allow
// the user to create a request and say where he has to inject the text to be passed to him
pub const ENVIRONMENT_VARIABLE_TEXT_TRANSLATE: &str = "${text_translate}";

// Defines the variable the nomenclature of the language variable to be translated to allow
// the user to create a request and say where he has to inject the language to be passed to him
pub const ENVIRONMENT_VARIABLE_LANGUAGE_TRANSLATE: &str = "${language_translate}";
